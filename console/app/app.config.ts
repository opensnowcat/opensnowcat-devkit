export default defineAppConfig({
  ui: {
    colors: {
      primary: 'orange',
      neutral: 'zinc',
      success: 'emerald',
      error: 'rose'
    },
    button: {
      slots: { base: 'cursor-pointer' }
    }
  }
})
